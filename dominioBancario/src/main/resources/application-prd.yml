spring:
  datasource:
    url: jdbc:postgresql://${PGHOST}:${PGPORT}/${PGDATABASE}
    username: ${PGUSER}
    password: ${PGPASSWORD}
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate

   #  para conectar com Railwai abrir arquivos de configuração e execução
   # duplicar o application DEV e renomear para application PRD e customizar as variáveis de ambiente colocando as informações fornecidas pelo Railway